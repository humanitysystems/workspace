# Managed checkouts, feature worktrees, and the local secrets vault
# are gitignored: they live in this workspace but never enter its history.
repos/
worktrees/
secrets/

# Local tool caches and dev logs
.freebuff/
*.log
demo-pids.txt
opencode.json
